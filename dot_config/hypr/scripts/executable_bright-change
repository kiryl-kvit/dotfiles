#!/bin/bash
# bright-change: adjust backlight and send level to wob
# Usage: bright-change +5% | 5%-

WOB_PIPE=/tmp/wob_pipe

brightnessctl set "$1"

pct=$(brightnessctl -m | awk -F, '{print $4}' | tr -d '%')
echo "$pct" > "$WOB_PIPE"
