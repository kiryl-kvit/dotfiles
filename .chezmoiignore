helpers/
docs/

.config/nvim/lazy-lock.json

# KDE/KWin settings - only apply if KDE is installed
{{- if not (lookPath "kwin_x11") }}
{{- if not (lookPath "kwin_wayland") }}
.config/kwinrc
.config/kglobalshortcutsrc
.config/kwinrulesrc
{{- end }}
{{- end }}
